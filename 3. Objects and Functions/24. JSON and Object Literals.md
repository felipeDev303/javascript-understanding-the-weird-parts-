# JSON y Object Literals en JavaScript

## 1. ¿Qué es un **Object Literal**?

Un **Object Literal** es una forma directa de crear objetos en JavaScript utilizando llaves `{}`.
Se usa para representar datos estructurados mediante pares **clave: valor**.

### **Características**

- Es parte del lenguaje JavaScript.
- Permite funciones, métodos y lógica.
- Los valores pueden ser cualquier tipo de dato de JS.
- Se evalúa como código JavaScript.

### **Ejemplo de Object Literal**

```js
const persona = {
  nombre: "Ana",
  edad: 25,
  activo: true,
  saludar() {
    console.log("Hola");
  },
};
```

---

## 2. ¿Qué es **JSON**?

**JSON (JavaScript Object Notation)** es un **formato de intercambio de datos**, basado en texto y completamente independiente del lenguaje.

### **Características**

- No ejecuta lógica → solo contiene datos.
- Es compatible con casi todos los lenguajes.
- Útil para enviar datos por API.
- Más estricto que un Object Literal.

### **Ejemplo JSON válido**

```json
{
  "nombre": "Ana",
  "edad": 25,
  "activo": true
}
```

---

## 3. Diferencias clave entre JSON y Object Literals

| Aspecto             | Object Literal             | JSON                                                 |
| ------------------- | -------------------------- | ---------------------------------------------------- |
| Forma               | Código JS                  | Texto plano                                          |
| Permite funciones   | Sí                         | No                                                   |
| Permite comentarios | Sí                         | No                                                   |
| Comillas            | Pueden no usarse en claves | Obligatoriamente dobles `"`                          |
| Uso principal       | Crear objetos en JS        | Transferir datos entre sistemas                      |
| Tipos permitidos    | Todos los de JS            | Solo: string, number, boolean, null, arrays, objetos |

---

## 4. Conversión entre JSON y Objetos

### De JSON → a objeto JavaScript

```js
const json = '{ "nombre": "Ana", "edad": 25 }';
const obj = JSON.parse(json);
```

### De objeto → a JSON

```js
const persona = { nombre: "Ana", edad: 25 };
const json = JSON.stringify(persona);
```

---

## 5. Ejemplos comparativos

### Object Literal con funciones y variables no permitidas en JSON

```js
const usuario = {
  nombre: "Luis",
  fecha: new Date(),
  activo: true,
  saludar() {
    console.log("Hola");
  },
};
```

### JSON equivalente (válido solo con datos)

```json
{
  "nombre": "Luis",
  "fecha": "2025-01-01T00:00:00.000Z",
  "activo": true
}
```

---

## 📌 Resumen Completo (Puntos Clave)

- **Object Literals** son objetos creados directamente en JavaScript mediante `{}`.
- Pueden contener **funciones, métodos, símbolos, fechas** y cualquier tipo de dato JS.
- **JSON** es un formato **solo de datos**, usado para comunicar información entre sistemas.
- JSON es **más estricto**:

  - Solo permite tipos primitivos, arrays y objetos simples.
  - No permite funciones ni comentarios.
  - Requiere comillas dobles en claves.

- Para usar JSON en JavaScript se convierte con:

  - `JSON.parse()` → JSON a objeto
  - `JSON.stringify()` → objeto a JSON

- Object Literals son parte del código ejecutable; JSON es un formato de almacenamiento/intercambio.
- JSON es fundamental para APIs, bases de datos NoSQL (como MongoDB), configuración y transporte de datos.

---

---

Anterior tema: [Framework Aside - Faking Namespaces](./23.%20Framework%20Aside%20-%20Faking%20Namespaces.md) | Siguiente tema: [Functions are Objects](./25.%20Functions%20are%20Objects.md) | Volver al índice: [Índice Temático](../../README.md)
