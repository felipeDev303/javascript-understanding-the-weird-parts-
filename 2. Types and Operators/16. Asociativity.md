# ⚙️ Operator Precedence (Precedencia de Operadores)

La **precedencia** determina **qué operador se evalúa primero** cuando hay varias operaciones en una misma expresión.

Ejemplo:

```js
2 + 3 * 4;
```

El `*` tiene mayor precedencia que `+`, por lo tanto:

```
3 * 4 → 12
2 + 12 → 14
```

---

## 📌 Tabla resumida de precedencia (de mayor a menor)

| Nivel | Operador              | Ejemplos                        |     |           |
| ----- | --------------------- | ------------------------------- | --- | --------- |
| 19    | `()`                  | agrupar expresiones             |     |           |
| 18    | `.` `[]` `new`        | acceso a propiedades, arrays    |     |           |
| 17    | `()`                  | llamada a función               |     |           |
| 16    | `++` `--` (post)      | incremento/decremento           |     |           |
| 15    | `!` `+` `-` `typeof`  | operadores unarios              |     |           |
| 14    | `**`                  | exponentes                      |     |           |
| 13    | `*` `/` `%`           | multiplicación, división, resto |     |           |
| 12    | `+` `-`               | suma, resta                     |     |           |
| 11    | `<<` `>>`             | desplazamiento de bits          |     |           |
| 10    | `<` `<=` `>` `>=`     | comparaciones                   |     |           |
| 9     | `==` `!=` `===` `!==` | igualdad                        |     |           |
| 8     | `&&`                  | AND lógico                      |     |           |
| 7     | `                     |                                 | `   | OR lógico |
| 4     | `=` `+=` `-=`         | asignación                      |     |           |
| 3     | `? :`                 | ternario                        |     |           |

**Cuando dudes, usa siempre paréntesis.**

---

## 🔁 Associativity (Asociatividad)

La **asociatividad** indica **la dirección** en la que se procesan los operadores **cuando tienen la misma precedencia**.

Hay dos tipos:

---

### ➡️ Left-to-Right (izquierda a derecha)

La mayoría de los operadores binarios:

- `+ - * / %`
- `== !==`
- `&& ||`
- `< > <= >=`

Ejemplo:

```js
10 - 5 - 2;
```

Se evalúa así:

```
(10 - 5) - 2 → 5 - 2 → 3
```

---

### ⬅️ Right-to-Left (derecha a izquierda)

Operadores como:

- `=`
- `+= -=`
- `!` (unario)
- `typeof`
- `**` (exponenciación)

Ejemplo con asignación:

```js
a = b = c = 10;
```

Se evalúa así:

```
c = 10
b = 10
a = 10
```

Ejemplo con exponentes:

```js
2 ** (3 ** 2);
```

Se evalúa como:

```
3 ** 2 → 9
2 ** 9 → 512
```

---

## 📝 Resumen final

- **Precedence** = qué operador se evalúa primero.
- **Associativity** = en qué dirección se evalúan operadores con la misma precedencia.
- `*` y `/` se ejecutan antes que `+` y `-`.
- La asignación (`=`) se evalúa de derecha a izquierda.
- El operador `**` (exponente) también es right-to-left.
- Usar paréntesis evita errores de interpretación.

---

+Anterior Tema: [14. Operator Precedence](14.%20Operator%20Precedence.md) | Siguiente Tema: [16. Coercion](16.%20Coercion.md) | Volver al Índice: [Índice Temático](./README.md)
