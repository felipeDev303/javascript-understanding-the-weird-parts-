# 🟦 Dynamic Typing en JavaScript

Big Word Alert

## 🟩 1. ¿Qué es _Dynamic Typing_?

**JavaScript es un lenguaje de tipado dinámico.**
Esto significa:

> **Las variables NO tienen tipo. Los valores sí.**

En otros lenguajes (Java, C#, Go), declaras así:

```java
int x = 10;
```

Aquí **x debe ser siempre un int**.

En JavaScript:

```js
let x = 10; // number
x = "hola"; // ahora es string
x = true; // ahora es boolean
```

La misma variable puede contener valores de cualquier tipo.

---

## 🟧 2. Modelo mental: “El vaso vacío”

Imagina que **las variables son vasos**
y **los valores son líquidos** con distintos colores.

- En Java y C++ → el vaso dice “vaso para agua” (int) o “vaso para café” (string).
  No puedes cambiar ese vaso.

- En JavaScript → el vaso es **transparente y flexible**.
  Hoy tiene agua, mañana tiene jugo, pasado tiene sopa.

> **La variable no impone qué tipo de valor puede contener.**

---

## 🟦 3. ¿Dónde ocurre realmente el tipado dinámico?

### En el momento de _asignar_ el valor.

Cuando JS evalúa:

```js
let x = 3;
```

- JS crea la variable
- pero **el objeto number 3** es lo que tiene tipo

Si luego haces:

```js
x = "3";
```

El vaso ahora contiene un string.
No hubo error → porque **el vaso no tiene tipo**.

---

## 🟩 4. La consecuencia más importante: _Type Coercion_

Dynamic typing le permite a JS intentar convertir valores:

```js
"5" * 2; // 10
"5" + 2; // "52"
true + 1; // 2
```

¿Por qué ocurre esto?

Porque JavaScript intenta interpretar los tipos _según el contexto_.

> **La dinámica del tipo abre la puerta a conversiones automáticas — útiles pero peligrosas.**

---

## 🟧 5. Tipado dinámico ≠ tipado débil

Es común confundirlos, pero:

- **Dynamic Typing** → tipo decidido en tiempo de ejecución.
- **Weak Typing** → permite convertir tipos de forma implícita.

JavaScript tiene **ambas características**.

Por eso hace cosas como:

```js
"10" - 1; // 9
"10" + 1; // "101"
```

Porque en `+` da prioridad a strings,
pero en `-` da prioridad a números.

---

## 🟦 6. Ventajas del Dynamic Typing

✔ Rápido de escribir
✔ Flexible
✔ Ideal para prototipado
✔ Perfecto para JSON y datos cambiantes
✔ Permite funciones muy genéricas

Ejemplo:

```js
function logger(x) {
  console.log(x);
}
```

`logger()` funciona con cualquier tipo.

---

## 🟥 Desventajas del Dynamic Typing

❌ Introduce errores difíciles de detectar
❌ Las funciones pueden recibir tipos inesperados
❌ Bugs silenciosos por coerción
❌ Menor predictibilidad
❌ Requiere más disciplina del desarrollador

Ejemplo clásico:

```js
function sum(a, b) {
  return a + b;
}

sum("1", 2); // "12" (bug oculto)
```

---

## 🟩 7. Cómo JavaScript determina el tipo:

### El _ECMAScript Specification Type System_

Los tipos primitivos:

- `number`
- `string`
- `boolean`
- `undefined`
- `null`
- `bigint`
- `symbol`

Y todo lo demás es **object**:

```js
typeof []; // "object"
typeof {}; // "object"
typeof null; // "object" (quirk histórico)
typeof (() => {}); // "function"
```

---

## 🟧 8. El verdadero truco: Dynamic Typing se combina con el Scope

Una variable puede cambiar de tipo en cualquier momento, pero **solo donde su scope lo permita**.

Ejemplo:

```js
let value = 10; // number

{
  let value = "hola"; // string (otro scope)
}

console.log(value); // 10 (nunca cambió su tipo)
```

El “vaso flexible” existe,
pero puede haber muchos vasos dependiendo del bloque.

---

## 🟦 9. Dynamic Typing + Asynchronous Callbacks

Gran combinación peligrosa:

```js
let data = null;

fetch("/api/user")
  .then((res) => res.json())
  .then((result) => {
    data = result; // ahora es un objeto
  });

console.log(data); // ❌ null (todavía)
```

El tipo cambia **después**, pero solo dentro del callback.

> Modelo mental:
> La variable cambia de contenido,
> pero el _momento_ en que ocurre importa tanto como el tipo.

---

## 🟨 10. Resumen final

### ✔ JavaScript tiene **dynamic typing**

### ✔ Las variables NO tienen tipo, los valores sí

### ✔ Una variable puede cambiar de tipo en tiempo real

### ✔ Dynamic Typing permite:

- prototipar rápido
- flexibilidad brutal
- funciones genéricas

### ✔ Pero trae riesgos:

- coerción implícita
- bugs silenciosos
- resultados inesperados

### ✔ Modelo mental:

> **Las variables son vasos transparentes; los valores son líquidos.
> Pueden contener cualquier cosa. Cambian cuando tú les cambias el contenido.**

---

+Anterior tema: [Asynchronous Callbacks](./10.%20Asynchronous%20Callbacks.md) |
+Siguiente tema: [Primitive Types](./12.%20Primitive%20Types.md) | Volver al índice: [Índice Temático](./README.md) | Volver al inicio: [Inicio](./00.%20Introduction.md)
