# ⚙️ Operators en JavaScript

Los operadores son símbolos especiales que **realizan operaciones** sobre uno o más valores (operandos) y devuelven un nuevo valor.

> **Modelo mental base:**
> Los operadores son _“puentes”_ que conectan valores para producir un **nuevo valor**.

Ejemplo:

```js
3 + 2; // el puente "+" conecta 3 y 2 → devuelve 5
```

---

## 📌 1. Tipos principales de operadores

JavaScript tiene varios grupos:

1. **Aritméticos**
2. **Asignación**
3. **Comparación**
4. **Lógicos**
5. **String / Concatenación**
6. **Ternario**
7. **Type operators (`typeof`, `instanceof`)**
8. **Operadores especiales (spread, in, delete)**

Vamos uno por uno.

---

## ➕ 2. Operadores Aritméticos

| Operador | Ejemplo  | Función        |
| -------- | -------- | -------------- |
| `+`      | 3 + 2    | Suma           |
| `-`      | 3 - 2    | Resta          |
| `*`      | 3 \* 2   | Multiplicación |
| `/`      | 6 / 2    | División       |
| `%`      | 7 % 3    | Resto          |
| `**`     | 2 \*\* 3 | Potencia       |

Ejemplo:

```js
10 % 4; // 2
```

---

## 📝 Modelo mental: “Calculadora interna”

Los operadores aritméticos son literalmente una _mini calculadora_ dentro del motor de JS.

Cuando ve:

```js
10 + 20;
```

El motor construye una pequeña expresión, la evalúa y reemplaza el resultado inmediatamente.

---

## 🟦 3. Operadores de Asignación

| Operador | Significado         |
| -------- | ------------------- |
| `=`      | asignación          |
| `+=`     | suma y asigna       |
| `-=`     | resta y asigna      |
| `*=`     | multiplica y asigna |

Ejemplo:

```js
let x = 10;
x += 5; // x = x + 5
```

---

## 🔍 4. Operadores de Comparación

### Comparación “floja”

```js
==   // compara valor
!=   // distinto por valor
```

Ejemplo:

```js
"5" == 5; // true (coerción explícita)
```

### Comparación estricta (recomendada siempre)

```js
===  // compara valor y tipo
!==  // distinto en valor o tipo
```

Ejemplo:

```js
"5" === 5; // false
```

---

## 🧠 Modelo mental: “Máquina de preguntas”

Los operadores de comparación funcionan como si JS preguntara:

> “¿Estos dos valores son _exactamente_ iguales?”
> “¿O solo se parecen si los fuerzo?”

---

## 🟩 5. Operadores Lógicos

| Operador | Significado |     |           |
| -------- | ----------- | --- | --------- |
| `&&`     | AND lógico  |     |           |
| `        |             | `   | OR lógico |
| `!`      | NOT lógico  |     |           |

Ejemplos:

```js
true && false; // false
true || false; // true
!true; // false
```

🎯 Super importante en JS:
`&&` y `||` **no devuelven true/false**, sino uno de los valores evaluados.

Ejemplo real:

```js
console.log("JS" && 42);
// → 42
```

Y:

```js
console.log("" || "fallback");
// → "fallback"
```

---

## 🧠 Modelo mental: “Puertas lógicas”

- `&&` es una **puerta** que solo deja pasar valores si todos son “truthy”.
- `||` es una puerta que deja pasar el **primer truthy** que encuentre.

Esto explica su uso para _default values_:

```js
let nombre = usuario || "Invitado";
```

---

## 🔗 6. Operadores de String (Concatenación)

Si uno de los operandos es un string, `+` se convierte en concatenador:

```js
"Hola " + "Mundo";
```

Sí:
`+` tiene **dos roles dependiendo del contexto**.

---

## ❓ 7. Ternario

Es un operador que reemplaza un `if` sencillo.

```js
condicion ? valorSiTrue : valorSiFalse;
```

Ejemplo:

```js
let acceso = edad >= 18 ? "Permitido" : "Denegado";
```

---

## 🧪 8. Type Operators

### `typeof`

Dice el tipo de un valor primitivo.

```js
typeof 5; // "number"
typeof "hola"; // "string"
```

Curiosidades:

```js
typeof null; // "object" (bug histórico de JS)
```

### `instanceof`

Sirve para saber si un objeto fue creado por un constructor:

```js
obj instanceof Array;
```

---

## ✨ 9. Otros Operadores Útiles

### Spread (...)

```js
let copia = [...array];
```

### in

```js
"nombre" in usuario;
```

### delete

```js
delete persona.edad;
```

---

## 🧠 Modelo Mental Global para entender **todos** los operadores

### 🧱 “Operadores como Transformers”

Piensa en los operadores como máquinas que:

- **toman uno o dos valores**
- **los transforman**
- **devuelven un nuevo valor**

Ejemplo:

```js
let z = (10 + 20) * 2;
```

Se ejecuta:

1. máquina `+` → 10 y 20 → devuelve 30
2. máquina `*` → 30 y 2 → devuelve 60
3. ese valor final se guarda en `z`

Este modelo mental evita memorizar y permite _inferir_ lo que hace una expresión.

---

## 📌 Resumen ultra rápido

| Tipo        | Ejemplo            | Para qué sirve     |     |                      |
| ----------- | ------------------ | ------------------ | --- | -------------------- |
| Aritméticos | `+ - * / % **`     | Matemática         |     |                      |
| Asignación  | `= += -= ...`      | Guardar valores    |     |                      |
| Comparación | `=== !=`           | Preguntar igualdad |     |                      |
| Lógicos     | `&&                |                    | !`  | Flujos condicionales |
| Ternario    | `? :`              | Condicional inline |     |                      |
| typeof      | `typeof x`         | Saber tipo         |     |                      |
| instanceof  | `obj instanceof A` | Saber origen       |     |                      |
| Spread      | `...arr`           | Copias y mezcla    |     |                      |
| delete      | `delete obj.prop`  | Quitar propiedades |     |                      |

---

+Anterior Tema: [12. Primitive Types](./12.%20Primitive%20Types.md) | Siguiente Tema: [15. Asociativity](./15.%20Asociativity.md) | Índice Temático: [README.md](./README.md)
