# ⚖️ Operator Precedence en JavaScript

## Cómo decide JS qué operación ejecutar primero

Cuando JavaScript evalúa una expresión, a menudo encuentra **varios operadores** al mismo tiempo:

```js
let x = 10 + 20 * 3;
```

¿Se suma primero?
¿Se multiplica?
¿Se asigna?

La respuesta depende de **Operator Precedence** (precedencia de operadores).

---

## 🎯 ¿Qué es Operator Precedence?

Es el conjunto de **reglas internas del lenguaje** que indican:

> “Qué operador se evalúa primero cuando hay varios en la misma expresión.”

Funciona exactamente igual a las matemáticas (multiplicación antes que suma), pero con muchos más operadores.

---

## 📌 Precedence simplificado (de mayor a menor)

Aquí tienes una versión muy práctica (no exhaustiva, pero suficiente para el 98% del código cotidiano).

### 🔺 Nivel más alto: _Primero se evalúan…_

#### 1. **Paréntesis** – nivel supremo

```js
(11 + 2) * 3;
```

#### 2. **Acceso y llamada**

- `obj.prop`
- `arr[index]`
- `func()`
- `new X()`

```js
persona.nombre;
usuarios[0];
sumar(2, 4);
```

#### 3. **Unary operators**

- `!`
- `typeof`
- `+valor`
- `-valor`

```js
!true;
typeof 10;
```

#### 4. **Multiplicación, división y módulo**

```js
*, /, %
```

#### 5. **Suma y resta**

```js
+, -
```

---

### 🔻 Nivel medio

#### 6. **Comparaciones**

```js
<, >, <=, >=
```

#### 7. **Igualdad**

```js
==, !=, ===, !==
```

#### 8. **AND lógico**

```js
&&
```

#### 9. **OR lógico**

```js
||
```

---

### 🔻 Nivel bajo

#### 10. **Ternario**

```js
cond ? a : b;
```

#### 11. **Asignación**

```js
=, +=, -=, *=, /=
```

---

## 🎓 Ejemplos que fijan el concepto

### 🧮 Ejemplo 1

```js
10 + 20 * 3;
```

Evaluación:

1. `20 * 3`
2. `10 + 60`
3. `70`

---

### 🧮 Ejemplo 2

```js
2 + 3 > 10;
```

Evaluación:

1. `2 + 3` → 5
2. `5 > 10` → false

---

### 🧮 Ejemplo 3

```js
true || (false && false);
```

Evaluación:

1. `false && false` → false
2. `true || false` → true

---

### 🧮 Ejemplo 4

```js
3 + 2 * 4 > 10 && 5 === "5";
```

Paso a paso:

1. `2 * 4` → 8
2. `3 + 8` → 11
3. `11 > 10` → true
4. `5 === "5"` → false
5. `true && false` → false

---

# 🔍 Precedence vs Associativity

_(conceptos hermanos)_

Cuando **dos operadores tienen la misma precedencia**, JS usa **Associativity** para decidir en qué dirección evaluarlos.

Ejemplo:

```js
10 - 2 - 1;
```

Para `-`:

- misma precedencia
- asociatividad: **izquierda a derecha**

Evaluación:

1. `(10 - 2) - 1`

Ejemplo de asociatividad al revés (right-to-left):

```js
x = y = z = 5;
```

Evaluación:

1. `z = 5`
2. `y = z`
3. `x = y`

---

# 🧠 Modelo Mental Complementario

### _“Precedence es la jerarquía, Associativity es la dirección”_

- Precedence → _quién manda primero_
- Associativity → _en qué dirección se procesa_

Con esto, incluso expresiones complejas se pueden leer casi automáticamente.

---

# 📌 Resumen ultra rápido

| Concepto                  | Significado                |
| ------------------------- | -------------------------- | ---- | ----------------------- |
| **Precedence**            | prioridad entre operadores |
| **Associativity**         | dirección de evaluación    |
| **Paréntesis**            | superan toda precedencia   |
| **Multiplicación > Suma** | igual que en matemáticas   |
| \*\*&& antes que          |                            | \*\* | AND tiene más prioridad |

---

# 🚀 ¿Deseas continuar?

Puedo crear el siguiente Markdown sobre:

✔️ **Coerción de tipos** (un tema clave tras operadores)
✔️ **Truthy & Falsy**
✔️ **Expressions vs Statements**
✔️ **Short-circuit evaluation**
✔️ **Cómo el motor evalúa expresiones paso a paso**

Indique cuál desea abordar.
