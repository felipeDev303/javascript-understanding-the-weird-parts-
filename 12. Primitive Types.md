# 🧩 Primitive Types en JavaScript

JavaScript trabaja con dos grandes categorías de valores:

- **Primitivos (Primitive Types)**
- **Objetos (Object Types)**

Los **primitivos** son la base más simple y fundamental del lenguaje: **valores inmutables, de tamaño fijo y almacenados directamente en memoria** (stack, según la implementación).
No tienen comportamiento complejo y no se modifican: **se reemplazan**.

---

## ✅ ¿Cuáles son los Primitive Types?

JavaScript tiene **7 tipos primitivos**:

1. **string**
2. **number**
3. **boolean**
4. **undefined**
5. **null**
6. **symbol**
7. **bigint**

---

## 🎯 1. `string`

Representan texto.

```js
let nombre = "Felipe";
```

- Siempre entre comillas.
- Son **inmutables** (no puedes cambiar un carácter, solo crear uno nuevo).

---

## 🎯 2. `number`

JavaScript tiene **un solo tipo numérico**, para enteros y decimales.

```js
let edad = 25;
let precio = 19.99;
```

Se basa en **IEEE 754** → a veces verás errores como:

```js
0.1 + 0.2; // 0.30000000000000004
```

---

## 🎯 3. `boolean`

Solo dos valores:

```js
true;
false;
```

Se usan para condiciones, comparaciones, lógica, etc.

---

## 🎯 4. `undefined`

Significa:

> "La variable existe, pero **no tiene valor asignado**."

Ejemplo:

```js
let x;
console.log(x); // undefined
```

---

## 🎯 5. `null`

Significa:

> "El valor es **intencionalmente vacío**."

```js
let usuario = null;
```

Diferencia clave:

| Tipo        | Significado                    |
| ----------- | ------------------------------ |
| `undefined` | falta de valor **por defecto** |
| `null`      | falta de valor **intencional** |

---

## 🎯 6. `symbol` (ES6+)

Sirve para crear **identificadores únicos**.

```js
let id = Symbol("id");
```

Incluso si creas dos símbolos iguales, no se igualan:

```js
Symbol("a") === Symbol("a"); // false
```

---

## 🎯 7. `bigint`

Para manejar números enormes que superan el límite de `number`.

```js
let big = 123456789012345678901234567890n;
```

---

## 🎓 ¿Qué hace especial a los tipos primitivos?

### ✨ Propiedad 1: **Inmutabilidad**

Los valores **no cambian**, se reemplazan:

```js
let a = 10;
a = 20; // a no cambia, se crea un nuevo valor
```

### ✨ Propiedad 2: **Paso por valor**

Cuando los usas en asignaciones o funciones:

```js
let x = 5;
let y = x;
y = 10;

console.log(x); // 5
```

Crear una copia exacta → no comparten memoria.

---

## 🧠 Modelo Mental

### 🧱 _Los Primitivos son "Ladrillos de Lego”_

Imagina tu memoria como una mesa y los primitivos como **ladrillos pequeños, indivisibles y sólidos**:

- No se pueden modificar → solo reemplazar.
- Copiarlos es fácil → se duplican como ladrillos.
- No contienen otros ladrillos dentro.
- Son la base sobre la que todo lo demás se construye.

En cambio, los **objetos** serían como **estructuras de Lego complejas**:

- Se pueden modificar parte por parte.
- Copiarlos requiere copiar referencias, no toda la estructura.
- Tienen propiedades dentro.

Esta metáfora funciona para recordar:

| Tipo      | Lego            | Comportamiento                |
| --------- | --------------- | ----------------------------- |
| Primitivo | ladrillo simple | inmutable, copia real         |
| Objeto    | nave/edificio   | mutable, copia por referencia |

---

## 🧪 Ejemplos para fijar el concepto

### ❗ ¿Por qué esto no funciona?

```js
let saludo = "hola";
saludo[0] = "H"; // no hace nada
```

Los strings son **inmutables**, así que debes crear uno nuevo:

```js
saludo = "Hola";
```

---

### 🎯 Paso por valor real

```js
let a = 3;
let b = a;
b++;

console.log(a); // 3
console.log(b); // 4
```

Cada uno es su propio ladrillo.

---

## 📝 Resumen Rápido

| Tipo      | Ejemplo    | Característica clave   |
| --------- | ---------- | ---------------------- |
| string    | `"hola"`   | texto inmutable        |
| number    | `42`       | único tipo numérico    |
| boolean   | `true`     | lógica                 |
| undefined | `let x;`   | sin valor              |
| null      | `null`     | vacío intencional      |
| symbol    | `Symbol()` | identificadores únicos |
| bigint    | `10000n`   | enteros enormes        |

---

+Anterior tema: [Dynamic Typing](./11.%20Dynamic%20Typing.md) | Siguiente tema: [Operators](./4.%20Operators.md) | Volver al índice: [Índice Temático](./README.md) | Volver al inicio: [Inicio](./00.%20Introduction.md)
